<% related_contacts.each do |contact| %>
<table class="note_data">
	<tr>
		<td class="avatar"><%= link_to avatar_to(contact, :size => "32"),  contact_url(contact), :id => "avatar" %></td>
		<td class="name" style="vertical-align: top;">
			<h4>
				<%= link_to contact.name, contact_url(contact) %>
			</h4>
			<%= contact.job_title %>
		</td>
	</tr>
</table>
<% end %>   
